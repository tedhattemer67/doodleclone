# Normalize all text files to LF in the repo and on checkout
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.woff2 binary

# Keep dev-only files out of `git archive` (the distributable plugin zip)
/.gitattributes export-ignore
/.gitignore    export-ignore
/tests         export-ignore
